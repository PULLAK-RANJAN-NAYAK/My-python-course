# Check the type of varible assigned using the input function

a = input("enter num 1 : ")
print(type(a))# as the input function are always string but you can use the conversion to print different data type