#It is an collection of well defined object 
# Both sets and dictionary are made by using curly brackets({})

e = set() # Don't use set{} as this will create an empty dictionary
value = {1,3,78,78,3,3,3,3,3} # In an set the value's are not repeated 

print(value)
